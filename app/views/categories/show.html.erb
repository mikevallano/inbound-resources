<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>


  <strong>Resources:</strong>
  <% @category.resources.each do |resource| %>
  <ul>
    <li>Name: <%= link_to "#{resource.name.titlecase}", resource_path(resource.id) %></li>
    <li>TLDR: <%= resource.tldr %></li>
    <li>Link: <%= resource.link %></li>
  </ul>
  <% end %>


<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
